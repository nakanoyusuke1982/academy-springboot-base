<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.springbootapplication.mapper.UserMapper">

    <insert id="insert" parameterType="com.spring.springbootapplication.entity.User">
        INSERT INTO users (
            name,
            email,
            password
        ) VALUES (
            #{name},
            #{email},
            #{password}
        )
    </insert>

   <select id="findByEmail"
        parameterType="java.lang.String"
        resultType="com.spring.springbootapplication.entity.User">
    SELECT
        id,
        name,
        email,
        password
    FROM users
    WHERE email = #{email}
</select>

</mapper>